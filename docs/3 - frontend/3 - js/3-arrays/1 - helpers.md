---
title: Простые действия
sidebar_position: 1
---

| Метод                            | Описание                            |
| -------------------------------- | ----------------------------------- |
| [`.includes()`](#includes)       | наличие примитива                   |
| [`.join-split()`](#join-split)   | преобразовать в строку и обратно    |
| [`.slice()`](#slice)             | обрезка                             |
| [`.splice()`](#splice)           | обрезка с заменой                   |
| [`.toSpliced()`](#tospliced)     | обрезка с заменой                   |
| [`.push()`](#push)               | добавить новый элемент в конец      |
| [`.unshift()`](#unshift)         | добавить новый элемент в начало     |
| [`.shift()`](#shift)             | удалить первый элемент              |
| [`.pop()`](#pop)                 | удалить последний элемент           |
| [`.concat()`](#concat)           | конкатенация массивов (объединение) |
| [`.indexOf()`](#indexof)         | найти индекс примитива              |
| [`.lastIndexOf()`](#lastindexof) | найти индекс примитива с конца      |
| [`.fill()`](#fill)               | заполнение массива                  |
| [`.flat()`](#flat)               | "плосколизация" одного уровня       |
| [`.reverse()`](#reverse)         | переворот                           |
| [`.toReversed()`](#toreversed)   | переворот                           |

---

## includes()

---

## join-split()

---

## slice()

---

## splice()

---

## toSpliced()

Возвращает новый массив, не мутирует исходный в отличие от `.splice()`.

---

## push()

---

## unshift()

---

## shift()

---

## pop()

---

## concat()

---

## indexOf()

---

## lastIndexOf()

---

## fill()

---

## flat()

---

## reverse()

---

## toReversed()

Возвращает новый массив, не мутирует исходный в отличие от `.reverse()`.
